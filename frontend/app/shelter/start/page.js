import ShelterStartWizard from './ShelterStartWizard';

export const metadata = {
  title: 'Get your free shelter account | ReunitePets',
  description:
    'Shelters and rescues get free pet management on ReunitePets: health records for every animal, automatic lost-pet matching, adoption handoffs, staff seats, and a public page. Apply in about a minute.',
};

export default function ShelterStartPage() {
  return <ShelterStartWizard />;
}
