/**
 * /geo/script: the script game's lobby. Ordinary page under the
 * universal bar and the game's subtabs; the round itself
 * (/geo/script/play) is the immersive one.
 */

import ScriptLobby from '../components/script/ScriptLobby';

export default function ScriptPage() {
  return <ScriptLobby />;
}
