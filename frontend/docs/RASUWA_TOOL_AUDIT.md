- **Anyone can ask for a mistake to be reviewed (founder ask,
  2026-09-02):** /rasuwa/correction is a public form: pick who it is
  about (or the letter overall), say what is wrong and what it should
  say instead, optionally leave a way to be reached. No account.
  Requests land in a new RasuwaCorrection table (rate limited,
  clipped by corrections.js) and show up on the task force board in a
  new Corrections section with Mark handled and Reopen, the same
  conditional-update pattern as the needs. The site never edits the
  list from a request: the families fix the letter document at its
  source and the next regeneration follows it, which is what the form
  and the board both say. Doors on every surface where details show:
  the wizard person step (with the picked person carried into
  ?for=num), the sign page, the letter page's header, and the
  progress chart.
